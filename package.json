{
  "name": "autumn-line",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5210 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:artifact": "ARTIFACT=1 vite build",
    "preview:artifact": "vite preview --outDir dist-artifact --port 5211 --strictPort",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.6"
  }
}
